import { useState } from 'react';

function App() {
  return (
    <div style={{ padding: '2rem', fontFamily: 'system-ui' }}>
      <h1>Placeholderer</h1>
      <p>Monorepo + schemas + core initialized.</p>
      <p>Next: Import / Job Overview UI</p>
    </div>
  );
}

export default App;